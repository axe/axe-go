package ux

// a wrapper for a labeled component (input, dropdown, etc)
// can have 3 components: label, component, bottom
// has multiple formats
//   single group vertical (label, component, bottom)
//   single group horizontal (label, component) or (component, label)
//   group horizontal with bottom (label & component on top, bottom on bottom)
