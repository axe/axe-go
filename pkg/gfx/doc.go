// Possible features
//
// - render transparent back to front
// - render opaque front to back
// - stacking shaders
// - z-prepass (used when pixel shading is expensive - render scene with default pixel shader to generate depth buffer)
// - deferred rendering (lighting is done in later pass)
// - pre-pass lighting
// - forward + (tiled forward rendering)
//
// Resources
// - https://tamats.com/learn/realtime-graphics/
package gfx
